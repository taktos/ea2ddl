# /---------------------------------------------------------------------------
# [Include Query] (NotRequired - Default 'map:{}')
#
map:{
    ; String = map:{
        ; NotInScope = map:{}
    }
    ; Number = map:{
        ; NotInScope = map:{}
    }
    ; Date = map:{
        ; NotEqual = map:{}
    }
}
# ----------------/
